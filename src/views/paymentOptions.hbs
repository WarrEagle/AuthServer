<!DOCTYPE html>
<html>
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
    <title>{{i18n "payment choices"}} - Friend Manager</title>
    <meta name="description" content="">
    <meta name="keywords" content="">
    <link rel="stylesheet" type="text/css" href="/stylesheets/bootstrap.min.css"/>
    <link rel="stylesheet" type="text/css" href="/stylesheets/style.css"/>
    <link rel="icon" type="image/png" href="/images/favicon.ico"/>
</head>
<body>
<div class="content container-fluid" style="text-align:center;">
    <h2>{{i18n "select payment provider"}}</h2>

    <p>{{i18n "upgrade to pro"}}</p>

    <p>{{{i18n "features" app.price app.currency_code}}}</p>

    <h3>{{i18n "don't delay"}}</h3>

    <div id="paymentOptions">
        {{#each paymentOptions}}
            {{#if isGoogleUser }}
                {{#isNotGoogleCheckout name}}
                    <a class='paymentOption'><img class='checkoutButton' src="{{buttonURL}}" onclick="initializeCheckout('{{name}}')"></a>
                {{/isNotGoogleCheckout}}
            {{else}}
                <a class='paymentOption'><img class='checkoutButton' src="{{buttonURL}}" onclick="initializeCheckout('{{name}}')"></a>
            {{/if}}
        {{/each}}
    </div>
    <br>

    <p>* {{{i18n "if you having trouble"}}}</p>

    <p>{{{i18n "support link" "http://kickasschromeapps.com/support/"}}}</p>
</div>
<script type='text/javascript'>
    function initializeCheckout(name) {
        window.location.replace('/initCheckout/' + name);
    }
</script>
<script>
    (function (i, s, o, g, r, a, m) {
        i['GoogleAnalyticsObject'] = r;
        i[r] = i[r] || function () {
            (i[r].q = i[r].q || []).push(arguments)
        }, i[r].l = 1 * new Date();
        a = s.createElement(o),
                m = s.getElementsByTagName(o)[0];
        a.async = 1;
        a.src = g;
        m.parentNode.insertBefore(a, m)
    })(window, document, 'script', '//www.google-analytics.com/analytics.js', 'ga');

    ga('create', 'UA-42859587-1', 'friend-manager.com');
    ga('send', 'pageview');

</script>
</body>
</html>
